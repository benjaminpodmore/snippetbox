services:
  db:
    container_name: mysql
    image: mysql
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
    ports:
      - "3306:3306"
    restart: unless-stopped
    volumes:
     - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
